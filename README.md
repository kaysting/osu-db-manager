# osu-db-manager

A Node.js library that unifies reading and writing osu! stable and lazer databases with a single API. Supports reading all database types and writing collections.

This library does not and will not support editing database values that aren't intended to be user-editable, like scores, beatmaps, or other internal values.
